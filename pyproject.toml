[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aio-agentic-rag"
version = "0.1.0"
description = "Benchmarking Agentic RAG on Vietnamese Legal Documents"
requires-python = ">=3.11"
dependencies = [
    # Config
    "pyyaml>=6.0",
    "python-dotenv>=1.0.0",

    # Progress bars
    "tqdm>=4.66.0",
]

[project.optional-dependencies]
api = [
    # Deep Agents + LangChain / LangGraph
    "deepagents>=0.6.1",
    "langchain>=1.3.0",
    "langchain-openai>=1.2.1",
    "langchain-community>=0.4.1",
    "langchain-chroma>=1.1.0",
    "langgraph>=1.2.0",
    "langgraph-checkpoint>=4.1.0",

    # Vector store
    "chromadb>=1.5.9",

    # BM25
    "rank-bm25>=0.2.2",

    # Reranker
    "sentence-transformers>=5.5.0",
    "transformers>=5.8.1",
    "torch>=2.10.0",

    # Graph
    "networkx>=3.3",

    # Evaluation
    "ragas>=0.4.3",
    "numpy>=1.26.0",
    "scikit-learn>=1.8.0",

    # FastAPI
    "fastapi>=0.136.1",
    "uvicorn[standard]>=0.47.0",
    "pydantic>=2.7.0",
]
ingest = [
    "datasets>=4.8.5",
    "huggingface-hub>=1.15.0",
    "beautifulsoup4>=4.12.0",
    "lxml>=5.0.0",
]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ipykernel>=6.29.0",
    "jupyter>=1.0.0",
]
ui = [
    "gradio>=4.0.0",
    "requests>=2.31.0",
    "matplotlib>=3.8.0",
    "pandas>=2.0.0",
]
gemini = [
    "langchain-google-genai>=4.0.0",
]
all = [
    "aio-agentic-rag[api,ingest,ui,dev,gemini]",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]